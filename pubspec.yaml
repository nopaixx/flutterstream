name: netflix_clone
description: A Netflix clone Flutter application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.8.6
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  webview_flutter: ^4.4.2
  webview_flutter_android: ^3.12.1  # 🆕 AÑADIDO para Android
  webview_flutter_wkwebview: ^3.9.4  # 🆕 AÑADIDO para iOS

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # 🆕 AÑADIDO: Assets para WebView Player
  assets:
    - assets/webview/